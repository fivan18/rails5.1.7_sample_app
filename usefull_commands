# to initialize guardfile
bundle exec guard init

# to execute guardfile
bundle exec guard

To exit Guard, press Ctrl-D

# To kill processes
ps aux | grep spring
kill -15 12241

# sometimes this doesn't work
spring stop

# kill all the processes whith name spring
pkill -15 -f spring

